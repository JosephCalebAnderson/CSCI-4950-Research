function getMexValue (currentGameState) {
    if (currentGameState == [1]) {
        return 0;
    } else {
        // get next moves of currentGameState and recursively get their Mex values
        // Use these values to get the Mex value of the current game state
    }
}